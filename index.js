import React from 'react';
import ReactDOM from 'react-dom/client';
import Forca from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
        <Forca/>
    </React.StrictMode>
);